# research-code

Install in editable mode:

```bash
pip install -e .
```

Then import from anywhere in the environment, for example:

```python
from research_code.starter import load_config
from research_code.pipelines import create_output_paths
```
